export default function Projects() {
  return (
    <div className="space-y-4">
      <div className="rounded-3xl border border-(--border) bg-(--surface) p-6 shadow-(--shadow-soft)">
        <div className="text-xs uppercase tracking-[0.3em] text-(--text-muted)">
          Workspace
        </div>
        <h1 className="mt-2 text-2xl font-semibold">Projects</h1>
        <p className="mt-2 text-(--text-muted)">
          Project management is not wired up yet, but the page route is now in place.
        </p>
      </div>
    </div>
  );
}
